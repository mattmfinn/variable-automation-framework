package com.theconstantvariable.pageobjectmodel.screenobjects;

/**
 * For all common methods that might be needed in either a Web or Mobile test
 */
public class BaseObject

        /**
         * Tests should be short and simple, testing one thing. This means I typically confirm to a one class per
         * test and @Test method. With test cases being precise with precise names, each failure
         * tells us a specific product-friendly way what specifically is broken. It will also allow for better
         * integration of hooks later on to say, skip log in but test the rest of the app. Test independence.
         *
         * So we use SoftAssert
         */

{
}
